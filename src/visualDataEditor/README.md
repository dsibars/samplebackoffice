# VisualDataEditor Module

This module provides a flexible interface to load, view, and directly manipulate data structures stored in CSV or JSON formats. It provides sorting capabilities and strict data preservation when overwriting original files or extracting them via the clipboard.
